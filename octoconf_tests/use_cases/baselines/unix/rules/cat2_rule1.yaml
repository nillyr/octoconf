id: "cat2_rule1"
title: "First rule of the second <x>category</x>"
description: |
  This is a <x>description</x>.
collection_cmd: |
  { id; whoami; } > user_info.txt
check: |
  whoami
verification_type: CHECK_REGEX
expected: .*
recommendation: |
  This is a long <x>recommendation</x>.
  Fixme reco step 1
  Fixme reco step 2
level: minimal
references:
  - https://example.com
  - https://example.com
